# Review and land

`agy` did the typing; you own the judgment. Re-run gates, read the diff, run the implementer sweep. You commit — never the implementer.

## Rework

```powershell
delegate-antigravity -Brief "Delta: use real fixture. Do not commit." -Continue -Dir F:\path\to\repo
```

Report design decisions and stop for scope changes beyond the brief.
